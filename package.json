{
  "license": "MIT",
  "private": true,
  "scripts": {
    "unit": "rm -rf output && abap_transpile abap_transpile.json && echo RUNNING && node output/index.mjs",
    "test": "npm run unit",
    "abaplint": "abaplint --format codeframe"
  },
  "repository": {
    "type": "git",
    "url": "github:miggi92/transport-tool"
  },
  "devDependencies": {
    "@abaplint/cli": "^2.94.7",
    "@abaplint/runtime": "^2.3.90",
    "@abaplint/transpiler-cli": "^2.3.90"
  }
}
